import DashboardLayout from "./(dashboard)/layout";

const Home = () => {
  return (
    <div>
      {/* Landing Page? */}
      {/* Authentication Page? */}
      <DashboardLayout>{/* Dashboard Page */}</DashboardLayout>
    </div>
  );
};

export default Home;
